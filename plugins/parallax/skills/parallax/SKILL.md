---
name: parallax
description: Control the parallax plugin — toggle on/off or check the log path
argument-hint: "[on|off|log]"
---

Execute the following command and report the output verbatim:

```
uv run --project "${CLAUDE_PLUGIN_ROOT}" parallax-skill $ARGUMENTS
```
